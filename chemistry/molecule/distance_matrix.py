import numpy as np
def get_distance_matrix(coordinate_list: list, system_name: str = 'cartesian') -> np.ndarry:
    """
    return distance matrix from input coordinations
    default return -1
    """
